refactor: 8 个 prompt 常量统一 include_str! 嵌入 + 重试 Q&A 漂移检测

延续前四次提交，本轮收尾两个细节：

1. 修复 prompts.rs 内部规范违反：
   - 之前 6 个 RECIPE_* 常量全是内联字符串（含 \n 转义），与模块顶部注释
     「所有 AI 系统提示词通过 include_str!() 从 resources/prompts/ 目录嵌入」
     直接矛盾
   - 6 个新文件迁移到 resources/prompts/：
     recipe_investigation.md / recipe_weekly.md / recipe_blueprint.md
     recipe_pcr.md / recipe_go_live.md / recipe_acceptance.md
     recipe_meeting.md
   - prompts.rs 全部 8 个常量改为 include_str!() 加载
   - 新增 test_all_constants_use_include_str 回归测试：
     扫描所有常量是否含 \\n 转义符（include_str! 不会转义，
     内联字符串会），防止后续有人再漂回到内联

2. 重试 Q&A 漂移检测：
   - handleGenerateReport 触发时计算 records 指纹 hash
     （id|answer|notes 拼接 + djb2 字符串 hash）存到 lastReportQaHashRef
   - handleRetryReport 重新计算 hash 并对比
   - 不一致时 toast.warning 提示用户「调研记录已变更，重试将沿用
     原 prompt（旧 Q&A 快照）」
   - 不阻塞重试——用户已了解风险仍想用旧 prompt 重发

至此 prompts 模块的同源化工作彻底闭环：
- 前端 ↔ 后端：get_investigation_recipe 命令拉取
- prompts.rs 内部：include_str! 嵌入
- SKILL.md ↔ prompt：4 段配方对齐

任何提示词修改只需改对应 .md 文件，无需改 Rust 或 TS 代码。

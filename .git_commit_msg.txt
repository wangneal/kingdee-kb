feat: 调研报告配方同源化、保存反馈优化、按钮空态

延续前三次提交，本轮收尾最后三个质量细节：

1. RECIPE 漂移防御：
   - 后端 commands::research 新增 get_investigation_recipe 命令，
     直接返回 services::prompts::RECIPE_INVESTIGATION 常量
   - lib.rs 注册命令
   - 前端 tauri-commands.ts 封装 fetchInvestigationRecipe()
   - ResearchAssistant.tsx 启动时 useEffect 拉取，存为 reportRecipe state
   - 移除 REPORT_RECIPE 硬编码字符串，buildReportPrompt 用 state
   - 漂移后果：如果后端改 4 段结构，前端 prompt 自动跟随，
     之前需要前后端同步改两份，现在只改 prompts.rs 一处

2. 保存报告反馈：
   - export_report 后端早已返回 file_path 字符串（core.rs:187-191）
   - 前端 invoke 之前没用返回值（直接 await 没用）
   - 改为 const savedPath = await invoke<string>("export_report", ...)
   - toast.success(`已保存到：${savedPath}`) 弹绝对路径

3. 空记录按钮禁用：
   - 「生成调研报告」按钮 disabled 条件从 aiLoading 扩展为
     aiLoading || records.length === 0
   - 加 title 属性提示原因（"暂无调研记录" / "基于当前调研记录生成 4 段结构报告"）
   - handleGenerateReport 内的 toast.warning 变为兜底（防御性保留）

4. e2e mock：tauri-mock.ts 添加 get_investigation_recipe 占位响应

至此调研报告功能闭环完成，3 + 1 = 4 次提交覆盖：
- 反模式删除 + 走 LLM/skill
- 落盘保存
- SKILL.md / 4 段配方 / 失败重试 / 上下文横幅
- 配方同源 / 路径反馈 / 空态禁用
